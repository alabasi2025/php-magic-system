# Code Translator v3.15.0 - خطة التصميم

## نظرة عامة
نظام ترجمة أكواد ذكي يستخدم Manus AI لترجمة الأكواد بين PHP واللغات الأخرى (Python, JavaScript, Java, C#).

## المعمارية

### 1. الطبقات الأساسية

#### Service Layer
- **CodeTranslatorService.php** - الخدمة الرئيسية
  - `translateCode($code, $fromLang, $toLang)` - ترجمة الكود
  - `detectLanguage($code)` - كشف اللغة تلقائياً
  - `validateSyntax($code, $language)` - التحقق من صحة الكود
  - `getTranslationHistory()` - سجل الترجمات
  - `compareTranslations($original, $translated)` - مقارنة الأكواد

#### Controller Layer
- **DeveloperController.php** - إضافة Methods جديدة:
  - `getAiCodeTranslatorPage()` - عرض الصفحة
  - `translateCodeWithAi()` - تنفيذ الترجمة

#### View Layer
- **resources/views/developer/ai/code-translator.blade.php**
  - واجهة مستخدم تفاعلية
  - محرر أكواد بـ syntax highlighting
  - عرض النتائج جنباً إلى جنب

### 2. الميزات الأساسية

#### أ. الترجمة الثنائية الاتجاه
- PHP → Python
- PHP → JavaScript
- PHP → Java
- PHP → C#
- Python/JavaScript/Java/C# → PHP

#### ب. الكشف التلقائي
- كشف لغة البرمجة تلقائياً
- اقتراح اللغة المستهدفة
- التحقق من صحة الكود قبل الترجمة

#### ج. التحليل والمقارنة
- عرض الكود الأصلي والمترجم جنباً إلى جنب
- تسليط الضوء على الفروقات
- شرح التغييرات المهمة

#### د. الذكاء الاصطناعي
- استخدام Manus AI للترجمة الذكية
- الحفاظ على المنطق والبنية
- تحسين الكود المترجم
- إضافة تعليقات توضيحية

### 3. اللغات المدعومة

#### PHP
- Laravel Syntax
- Eloquent ORM
- Blade Templates
- PHP 8.x Features

#### Python
- Django/Flask Patterns
- SQLAlchemy
- Jinja2 Templates
- Python 3.x Features

#### JavaScript
- Node.js/Express
- Sequelize/Mongoose
- ES6+ Features
- TypeScript Support

#### Java
- Spring Boot
- Hibernate
- Java 17+ Features
- Maven/Gradle

#### C#
- .NET Core/ASP.NET
- Entity Framework
- C# 10+ Features
- NuGet Packages

### 4. نظام Prompts للذكاء الاصطناعي

#### Template الأساسي
```
أنت خبير في ترجمة الأكواد البرمجية. قم بترجمة الكود التالي من {from_language} إلى {to_language}.

متطلبات الترجمة:
1. الحفاظ على المنطق والوظيفة الأصلية
2. استخدام أفضل الممارسات للغة المستهدفة
3. إضافة تعليقات توضيحية للأجزاء المعقدة
4. تحسين الأداء حيثما أمكن
5. الحفاظ على قابلية القراءة

الكود الأصلي ({from_language}):
```{from_language}
{code}
```

قم بتوفير:
1. الكود المترجم
2. ملاحظات حول التغييرات المهمة
3. اقتراحات للتحسين
```

#### Prompts متخصصة

##### PHP → Python
- تحويل Eloquent إلى SQLAlchemy
- تحويل Blade إلى Jinja2
- تحويل Middleware إلى Decorators

##### PHP → JavaScript
- تحويل Classes إلى ES6 Classes
- تحويل Eloquent إلى Sequelize
- تحويل Blade إلى React/Vue

##### PHP → Java
- تحويل Classes إلى Spring Components
- تحويل Eloquent إلى Hibernate
- تحويل Routes إلى Controllers

##### PHP → C#
- تحويل Classes إلى C# Classes
- تحويل Eloquent إلى Entity Framework
- تحويل Blade إلى Razor

### 5. واجهة المستخدم

#### Layout
```
┌─────────────────────────────────────────────────────────┐
│  Code Translator v3.15.0 - مترجم الأكواد               │
├─────────────────────────────────────────────────────────┤
│  [PHP ▼] → [Python ▼]  [🔄 Swap]  [🔍 Detect]         │
├──────────────────────┬──────────────────────────────────┤
│  Original Code       │  Translated Code                 │
│  ┌────────────────┐ │  ┌────────────────────────────┐  │
│  │                │ │  │                            │  │
│  │  // PHP Code   │ │  │  # Python Code             │  │
│  │                │ │  │                            │  │
│  │                │ │  │                            │  │
│  └────────────────┘ │  └────────────────────────────┘  │
├──────────────────────┴──────────────────────────────────┤
│  [📋 Copy] [💾 Save] [📥 Download] [🔄 Translate]      │
├─────────────────────────────────────────────────────────┤
│  Translation Notes:                                     │
│  • Converted Eloquent to SQLAlchemy                    │
│  • Added type hints for Python 3.10+                   │
│  • Optimized database queries                          │
└─────────────────────────────────────────────────────────┘
```

#### Features
- Split View (عرض منقسم)
- Syntax Highlighting (تلوين الكود)
- Line Numbers (أرقام الأسطر)
- Auto-detect Language (كشف تلقائي)
- Copy/Download (نسخ/تحميل)
- History (السجل)

### 6. قاعدة البيانات

#### جدول translation_history
```sql
CREATE TABLE translation_history (
    id BIGINT PRIMARY KEY,
    user_id BIGINT,
    from_language VARCHAR(50),
    to_language VARCHAR(50),
    original_code TEXT,
    translated_code TEXT,
    notes TEXT,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

### 7. API Integration

#### Manus AI Configuration
```php
[
    'api_url' => 'https://api.manus.ai/v1/tasks',
    'model' => 'manus-1.5-lite',
    'timeout' => 120,
    'max_tokens' => 4000,
]
```

### 8. Error Handling

#### أنواع الأخطاء
- Syntax Error (خطأ في الكود)
- Unsupported Language (لغة غير مدعومة)
- API Error (خطأ في الاتصال)
- Timeout Error (انتهاء المهلة)

#### الرسائل
- رسائل خطأ واضحة بالعربية
- اقتراحات للحل
- أمثلة صحيحة

### 9. Testing

#### Unit Tests
- اختبار ترجمة بسيطة
- اختبار كشف اللغة
- اختبار التحقق من الصحة

#### Integration Tests
- اختبار API Integration
- اختبار حفظ السجل
- اختبار واجهة المستخدم

### 10. Performance

#### Optimization
- Cache للترجمات المتكررة
- Async Processing للأكواد الكبيرة
- Pagination للسجل

#### Limits
- Max Code Size: 10,000 lines
- Max Translations per Day: 100
- Cache Duration: 24 hours

## خطة التنفيذ

### المرحلة 1: Service Layer (10 دقائق)
- إنشاء CodeTranslatorService
- تطبيق Methods الأساسية
- إضافة Prompts للذكاء الاصطناعي

### المرحلة 2: Controller Layer (5 دقائق)
- إضافة Methods في DeveloperController
- معالجة Requests
- معالجة Responses

### المرحلة 3: View Layer (10 دقائق)
- إنشاء Blade Template
- إضافة JavaScript للتفاعل
- إضافة CSS للتنسيق

### المرحلة 4: Testing (5 دقائق)
- اختبار الترجمة
- اختبار واجهة المستخدم
- إصلاح الأخطاء

## الملفات المطلوبة

1. **app/Services/AI/CodeTranslatorService.php** - الخدمة الرئيسية
2. **app/Http/Controllers/DeveloperController.php** - إضافة Methods
3. **resources/views/developer/ai/code-translator.blade.php** - واجهة المستخدم
4. **routes/developer.php** - Routes موجودة بالفعل (السطر 127-128)
5. **database/migrations/xxxx_create_translation_history_table.php** - Migration للسجل

## الإصدار
- **Version:** v3.15.0
- **Release Date:** 2025-12-03
- **Status:** In Development

---

**ملاحظة:** هذا التصميم يتبع نفس البنية المستخدمة في الميزات السابقة (Code Optimizer, Bug Detector, Documentation Generator) لضمان التناسق والتكامل.
