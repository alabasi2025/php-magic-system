# 🧬 Migration Generator v3.23.0 - دليل المستخدم الشامل

**آخر تحديث:** 2025-12-03  
**الإصدار:** v3.23.0  
**معد الدليل:** Manus AI

---

## 🎯 مقدمة

**Migration Generator** هو أداة ذكية وقوية ضمن نظام **PHP Magic System**، مصممة لتسريع وأتمتة عملية إنشاء ملفات الـ migrations في Laravel. بدلاً من كتابة الكود يدوياً، يمكنك الآن وصف الجدول الذي تريده باللغة العربية، أو تقديم مخطط JSON، أو استخدام قالب جاهز، وسيقوم النظام بتوليد الكود بالنيابة عنك بجودة عالية وتوثيق شامل.

### لمن هذا الدليل؟

هذا الدليل موجه للمطورين الذين يعملون على نظام **PHP Magic System** ويرغبون في استخدام **Migration Generator** لزيادة إنتاجيتهم وضمان جودة كود قاعدة البيانات.

---

## 🌟 الفوائد الرئيسية

- **🚀 السرعة:** قلل وقت كتابة الـ migrations بنسبة تصل إلى 80%.
- **✨ الجودة:** اضمن اتباع أفضل الممارسات والمعايير في كل migration.
- **🐛 تقليل الأخطاء:** اكتشف الأخطاء المحتملة قبل تطبيق الـ migration.
- **📚 التوثيق التلقائي:** احصل على توثيق شامل وتعليقات واضحة بالعربية لكل جدول وعمود.
- **🧠 الذكاء الاصطناعي:** استفد من اقتراحات الذكاء الاصطناعي لتحسين بنية الجداول.

---

## 🔑 كيفية الوصول

يمكنك الوصول إلى **Migration Generator** من خلال لوحة تحكم المطور الرئيسية، أو مباشرة عبر الرابط التالي:

```
/migration-generator
```

---

## 🖥️ شرح الواجهات

### 1. الصفحة الرئيسية (Dashboard)

هي نقطة البداية، حيث يمكنك رؤية جميع الـ migrations التي تم توليدها وإدارتها.

![صورة للوحة التحكم الرئيسية](https://via.placeholder.com/800x400.png?text=Migration+Generator+Dashboard)

**المكونات الرئيسية:**

| المكون | الوصف |
|---|---|
| **➕ إنشاء Migration جديد** | زر للانتقال إلى صفحة الإنشاء والبدء في توليد migration جديد. |
| **إحصائيات** | بطاقات تعرض ملخصاً لعدد الـ migrations في كل حالة (الإجمالي، مسودة، مولدة، إلخ). |
| **البحث والفلترة** | أدوات للبحث عن migration معين بالاسم أو فلترة النتائج حسب النوع أو الحالة. |
| **جدول الـ Migrations** | عرض جدولي لجميع الـ migrations المولدة مع تفاصيلها (الاسم، الجدول، النوع، الحالة، التاريخ). |
| **الإجراءات** | أزرار لإجراء عمليات سريعة على كل migration (عرض، تحميل، حذف). |

### 2. صفحة الإنشاء (Create New Migration)

هنا يحدث السحر! تتيح لك هذه الصفحة توليد الـ migrations بثلاث طرق مختلفة.

![صورة لصفحة الإنشاء](https://via.placeholder.com/800x500.png?text=Create+New+Migration)

**طرق التوليد:**

1.  **📝 وصف نصي (AI-Powered):**
    - **كيفية الاستخدام:** اكتب وصفاً باللغة العربية الطبيعية للجدول الذي تريد إنشاءه في صندوق النص.
    - **مثال:** `أريد إنشاء جدول للمستخدمين يحتوي على الاسم والبريد الإلكتروني وكلمة المرور.`
    - **نصيحة:** كلما كان الوصف أكثر تفصيلاً، كانت النتيجة أفضل. يمكنك تحديد أنواع الأعمدة، العلاقات، الفهارس، وغيرها.

2.  **📋 JSON Schema:**
    - **كيفية الاستخدام:** قم بلصق مخطط JSON يصف الجدول والأعمدة والفهارس بدقة.
    - **مثال:** يمكنك استخدام المثال الموجود في محرر الكود كنقطة بداية.
    - **الفائدة:** مثالي للعمليات الدقيقة والمحددة مسبقاً أو عند التكامل مع أنظمة أخرى.

3.  **🎨 من قالب (From Template):**
    - **كيفية الاستخدام:** اختر قالباً جاهزاً من القائمة (مثل: جدول منتجات، جدول فواتير)، ثم املأ المتغيرات المطلوبة (مثل اسم الجدول).
    - **الفائدة:** طريقة سريعة جداً لإنشاء جداول شائعة الاستخدام.

### 3. صفحة العرض والتعديل (Show & Edit)

بعد توليد الـ migration، ستنتقل إلى هذه الصفحة لمعاينة الكود وتعديله وحفظه.

![صورة لصفحة العرض](https://via.placeholder.com/800x600.png?text=Show+and+Edit+Migration)

**المكونات الرئيسية:**

| المكون | الوصف |
|---|---|
| **معلومات أساسية** | بطاقات تعرض معلومات سريعة عن الـ migration (النوع، الحالة، تاريخ الإنشاء). |
| **الكود المولد** | محرر كود متقدم (CodeMirror) يعرض الكود المولد مع تمييز للألوان. **يمكنك التعديل على الكود مباشرة.** |
| **أزرار الإجراءات** | - **📋 نسخ:** لنسخ الكود إلى الحافظة.
- **⬇️ تحميل:** لتحميل الكود كملف `.php`.
- **💾 حفظ كملف:** لحفظ الـ migration مباشرة في مجلد `database/migrations` في المشروع. |
| **💾 حفظ التعديلات** | لحفظ أي تغييرات قمت بها على الكود في محرر الكود. |
| **البيانات المدخلة** | عرض البيانات الأصلية التي تم استخدامها لتوليد الـ migration (سواء كانت نصاً أو JSON). |
| **اقتراحات الذكاء الاصطناعي** | (ميزة مستقبلية) ستعرض اقتراحات لتحسين الجدول. |

---

## ⚙️ عمليات تجريبية (Step-by-Step)

### عملية 1: إنشاء جدول منتجات باستخدام الوصف النصي

1.  اذهب إلى صفحة **Migration Generator** الرئيسية.
2.  اضغط على زر **➕ إنشاء Migration جديد**.
3.  في تبويب **📝 وصف نصي**، اكتب الوصف التالي:
    ```
    أريد إنشاء جدول للمنتجات (products) يحتوي على:
    - اسم المنتج (name) من نوع string
    - وصف المنتج (description) من نوع text ويكون nullable
    - السعر (price) من نوع decimal
    - الكمية في المخزون (stock_quantity) من نوع integer وقيمته الافتراضية 0
    - علاقة مع جدول الفئات (categories)
    ```
4.  اضغط على زر **🚀 توليد Migration**.
5.  ستنتقل إلى صفحة العرض. راجع الكود المولد.
6.  إذا كان كل شيء صحيحاً، اضغط على زر **💾 حفظ كملف**.
7.  **تهانينا!** لقد قمت بإنشاء وحفظ migration جديد في أقل من دقيقة.

### عملية 2: تعديل جدول باستخدام JSON

1.  اذهب إلى صفحة الإنشاء.
2.  اختر تبويب **📋 JSON Schema**.
3.  قم بلصق الـ JSON التالي:
    ```json
    {
      "table_name": "products",
      "description": "إضافة عمود للخصم لجدول المنتجات",
      "type": "alter",
      "columns": [
        {
          "name": "discount",
          "type": "decimal",
          "precision": 5,
          "scale": 2,
          "nullable": true,
          "after": "price",
          "comment": "قيمة الخصم"
        }
      ]
    }
    ```
4.  اضغط على زر **🚀 توليد من JSON**.
5.  راجع الكود المولد الذي سيحتوي على `Schema::table`.
6.  اضغط على **💾 حفظ كملف**.

---

## 🔌 استخدام الـ API

يمكنك أيضاً توليد الـ migrations برمجياً باستخدام الـ API.

### Endpoint: `POST /api/migration-generator/generate`

**Header:**
- `Authorization: Bearer <YOUR_API_TOKEN>`
- `Accept: application/json`

**Body (JSON):**
```json
{
  "schema": {
    "table_name": "api_generated_table",
    "description": "جدول تم توليده عبر الـ API",
    "type": "create",
    "columns": [
      {
        "name": "api_key",
        "type": "string",
        "length": 100,
        "unique": true
      },
      {
        "name": "usage_count",
        "type": "integer",
        "default": 0
      }
    ]
  }
}
```

### الاستجابة (Success):
```json
{
  "success": true,
  "data": {
    "id": 123,
    "name": "create_api_generated_table_table",
    "table_name": "api_generated_table",
    "content": "<?php ... // Generated migration content",
    "status": "generated"
  }
}
```

---

## ❓ الأسئلة الشائعة (FAQ)

**س: هل يمكنني تعديل الكود بعد توليده؟**
ج: نعم، يمكنك تعديل الكود مباشرة في محرر الكود في صفحة العرض قبل حفظه.

**س: هل يدعم النظام علاقات Many-to-Many؟**
ج: نعم، يمكنك وصف العلاقة في النص، والنظام سيقوم بإنشاء الجدول الوسيط (pivot table) تلقائياً.

**س: ماذا يحدث عند الضغط على "حفظ كملف"؟**
ج: يقوم النظام بإنشاء ملف `.php` بالاسم والتاريخ الصحيحين ويحفظه مباشرة في مجلد `database/migrations` الخاص بمشروعك.

**س: هل يمكنني حذف migration تم توليده؟**
ج: نعم، من الصفحة الرئيسية، يمكنك حذف أي migration. سيتم حذفه من سجلات النظام (Soft Delete) وحذف الملف الفعلي إذا كان قد تم إنشاؤه.

---

## 📞 الدعم الفني

إذا واجهت أي مشاكل أو كان لديك أي اقتراحات، يرجى فتح **Issue** في مستودع المشروع على GitHub.
