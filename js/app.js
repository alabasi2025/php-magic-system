import './bootstrap';
import { createApp } from 'vue';
import Alpine from 'alpinejs';

// Alpine.js
window.Alpine = Alpine;
Alpine.start();

// Vue.js (اختياري)
// const app = createApp({});
// app.mount('#app');
