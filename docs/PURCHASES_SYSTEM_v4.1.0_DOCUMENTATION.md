# نظام المشتريات v4.1.0 - التوثيق الشامل
## Purchase System v4.1.0 - Complete Documentation

---

## 📋 نظرة عامة | Overview

**نظام المشتريات v4.1.0** هو نظام متكامل لإدارة عمليات الشراء من الموردين، مع التكامل الكامل مع نظام المخازن والنظام المحاسبي. تم تطويره باستخدام **منهجية التوازي الذكي** لضمان أعلى معايير الجودة والأداء.

**Purchase System v4.1.0** is a comprehensive system for managing purchasing operations from suppliers, with full integration with the inventory and accounting systems. Developed using **Smart Parallel Methodology** to ensure the highest standards of quality and performance.

---

## 🎯 الميزات الرئيسية | Key Features

### 1. إدارة الموردين | Supplier Management
- ✅ إضافة وتعديل بيانات الموردين
- ✅ تتبع شروط الدفع (نقدي/آجل)
- ✅ حدود الائتمان ومدة السداد
- ✅ ربط الموردين بالحسابات المحاسبية

### 2. أوامر الشراء | Purchase Orders
- ✅ إنشاء أوامر شراء للموردين
- ✅ إضافة أصناف متعددة لكل أمر
- ✅ حساب تلقائي للضرائب والخصومات
- ✅ تتبع حالات الأوامر (مسودة، مرسل، معتمد، مستلم)
- ✅ اعتماد وإلغاء الأوامر

### 3. استلام البضاعة | Purchase Receipts
- ✅ استلام البضاعة مع أو بدون أمر شراء
- ✅ **التكامل التلقائي مع نظام المخازن**
- ✅ إنشاء حركات مخزون تلقائية عند الاعتماد
- ✅ تحديث الكميات المستلمة في أوامر الشراء

### 4. فواتير الموردين | Purchase Invoices
- ✅ إنشاء فواتير من الموردين
- ✅ **التكامل التلقائي مع النظام المحاسبي**
- ✅ إنشاء قيود محاسبية تلقائية عند الاعتماد
- ✅ تتبع حالات الدفع (غير مدفوع، مدفوع جزئياً، مدفوع)
- ✅ تسجيل الدفعات وإنشاء قيود الدفع

### 5. التقارير والإحصائيات | Reports & Analytics
- ✅ تقرير أوامر الشراء
- ✅ تقرير المشتريات حسب المورد
- ✅ تقرير المشتريات حسب الصنف
- ✅ تقرير الفواتير المستحقة
- ✅ تقرير أداء الموردين
- ✅ إحصائيات لوحة التحكم

---

## 🏗️ البنية التقنية | Technical Architecture

### Models (النماذج)
```
app/Models/
├── Supplier.php                    # نموذج الموردين
├── PurchaseOrder.php               # نموذج أوامر الشراء
├── PurchaseOrderItem.php           # نموذج أصناف أوامر الشراء
├── PurchaseReceipt.php             # نموذج استلام البضاعة
├── PurchaseReceiptItem.php         # نموذج أصناف استلام البضاعة
├── PurchaseInvoice.php             # نموذج فواتير الموردين
└── PurchaseInvoiceItem.php         # نموذج أصناف فواتير الموردين
```

### Migrations (قاعدة البيانات)
```
database/migrations/
├── 2025_12_06_000001_create_suppliers_table.php
├── 2025_12_06_000002_create_purchase_orders_table.php
├── 2025_12_06_000003_create_purchase_order_items_table.php
├── 2025_12_06_000004_create_purchase_receipts_table.php
└── 2025_12_06_000005_create_purchase_invoices_table.php
```

### Services (منطق الأعمال)
```
app/Services/
├── PurchaseOrderService.php        # خدمة أوامر الشراء
├── PurchaseReceiptService.php      # خدمة استلام البضاعة (مع التكامل مع المخازن)
├── PurchaseInvoiceService.php      # خدمة فواتير الموردين (مع التكامل المحاسبي)
└── PurchaseReportService.php       # خدمة التقارير
```

### Routes (المسارات)
```
routes/
└── purchases.php                   # جميع مسارات نظام المشتريات (40+ مسار)
```

### Tests (الاختبارات)
```
tests/Feature/
└── PurchasesSystemTest.php         # 18 اختبار شامل
```

---

## 🔄 التكامل مع الأنظمة الأخرى | System Integration

### 1. التكامل مع نظام المخازن | Inventory Integration

عند **اعتماد استلام البضاعة**، يتم تلقائياً:
- إنشاء حركة مخزون (StockMovement) من نوع `stock_in`
- تحديث كميات المخزون في المخزن المحدد
- ربط الحركة بإيصال الاستلام

```php
// في PurchaseReceiptService::createStockMovements()
StockMovement::create([
    'movement_type' => 'stock_in',
    'warehouse_id' => $receipt->warehouse_id,
    'item_id' => $item->item_id,
    'quantity' => $item->quantity,
    'unit_cost' => $item->unit_price,
    'reference_type' => 'purchase_receipt',
    'reference_id' => $receipt->id,
]);
```

### 2. التكامل مع النظام المحاسبي | Accounting Integration

عند **اعتماد فاتورة المورد**، يتم تلقائياً:
- إنشاء قيد محاسبي (JournalEntry)
- تسجيل المدين: حساب المشتريات + ضريبة المدخلات
- تسجيل الدائن: حساب المورد
- ربط القيد بالفاتورة

```php
// في PurchaseInvoiceService::createJournalEntry()
// مدين: المشتريات
JournalEntryDetail::create([
    'account_id' => $purchasesAccountId,
    'debit' => $amountAfterDiscount,
    'credit' => 0,
]);

// دائن: حساب المورد
JournalEntryDetail::create([
    'account_id' => $supplierAccountId,
    'debit' => 0,
    'credit' => $invoice->total_amount,
]);
```

عند **تسجيل دفعة**، يتم تلقائياً:
- إنشاء قيد محاسبي للدفعة
- تسجيل المدين: حساب المورد
- تسجيل الدائن: حساب الصندوق/البنك
- تحديث حالة الدفع في الفاتورة

---

## 📊 جداول قاعدة البيانات | Database Tables

### 1. جدول الموردين | suppliers
| الحقل | النوع | الوصف |
|-------|------|-------|
| id | bigint | المعرف الفريد |
| code | string | رمز المورد (فريد) |
| name | string | اسم المورد |
| contact_person | string | الشخص المسؤول |
| phone | string | رقم الهاتف |
| email | string | البريد الإلكتروني |
| payment_terms | enum | شروط الدفع (cash/credit) |
| credit_limit | decimal | حد الائتمان |
| credit_days | integer | مدة الائتمان بالأيام |
| account_id | foreign | الحساب المحاسبي |
| status | enum | الحالة (active/inactive) |

### 2. جدول أوامر الشراء | purchase_orders
| الحقل | النوع | الوصف |
|-------|------|-------|
| id | bigint | المعرف الفريد |
| order_number | string | رقم الأمر (فريد) |
| supplier_id | foreign | المورد |
| warehouse_id | foreign | المخزن المستلم |
| order_date | date | تاريخ الأمر |
| expected_date | date | تاريخ التسليم المتوقع |
| subtotal | decimal | المجموع الفرعي |
| tax_amount | decimal | قيمة الضريبة |
| discount_amount | decimal | قيمة الخصم |
| total_amount | decimal | المجموع الكلي |
| status | enum | الحالة (draft/sent/confirmed/received/cancelled) |
| payment_status | enum | حالة الدفع |

### 3. جدول استلام البضاعة | purchase_receipts
| الحقل | النوع | الوصف |
|-------|------|-------|
| id | bigint | المعرف الفريد |
| receipt_number | string | رقم الاستلام (فريد) |
| purchase_order_id | foreign | أمر الشراء (اختياري) |
| supplier_id | foreign | المورد |
| warehouse_id | foreign | المخزن |
| receipt_date | date | تاريخ الاستلام |
| status | enum | الحالة (pending/approved/rejected) |

### 4. جدول فواتير الموردين | purchase_invoices
| الحقل | النوع | الوصف |
|-------|------|-------|
| id | bigint | المعرف الفريد |
| invoice_number | string | رقم الفاتورة من المورد |
| internal_number | string | رقم داخلي (فريد) |
| supplier_id | foreign | المورد |
| invoice_date | date | تاريخ الفاتورة |
| due_date | date | تاريخ الاستحقاق |
| total_amount | decimal | المجموع الكلي |
| paid_amount | decimal | المبلغ المدفوع |
| remaining_amount | decimal | المبلغ المتبقي |
| payment_status | enum | حالة الدفع |
| status | enum | الحالة (draft/approved/cancelled) |
| journal_entry_id | foreign | القيد المحاسبي |

---

## 🔧 التثبيت والإعداد | Installation & Setup

### 1. تشغيل الهجرات | Run Migrations
```bash
php artisan migrate
```

### 2. تضمين ملف المسارات | Include Routes File
أضف السطر التالي في `routes/web.php`:
```php
require __DIR__.'/purchases.php';
```

### 3. إعداد الحسابات المحاسبية | Configure Accounting Accounts
أضف في `config/accounting.php`:
```php
return [
    'accounts' => [
        'purchases' => 1,      // حساب المشتريات
        'tax_input' => 2,      // حساب ضريبة المدخلات
        'cash' => 3,           // حساب الصندوق
        'bank' => 4,           // حساب البنك
    ],
];
```

---

## 🧪 الاختبارات | Testing

### تشغيل جميع الاختبارات | Run All Tests
```bash
php artisan test --filter PurchasesSystemTest
```

### الاختبارات المتوفرة (18 اختبار) | Available Tests
1. ✅ إنشاء مورد جديد
2. ✅ إنشاء أمر شراء
3. ✅ إضافة أصناف لأمر الشراء
4. ✅ حساب إجماليات الأمر
5. ✅ اعتماد أمر الشراء
6. ✅ إلغاء أمر الشراء
7. ✅ استلام بضاعة مع أمر شراء
8. ✅ استلام بضاعة بدون أمر شراء
9. ✅ اعتماد الاستلام وإنشاء حركة مخزون
10. ✅ إنشاء فاتورة مورد
11. ✅ اعتماد الفاتورة وإنشاء قيد محاسبي
12. ✅ تسجيل دفعة للفاتورة
13. ✅ حساب المبلغ المتبقي
14. ✅ منع حذف مورد له عمليات
15. ✅ تقرير أوامر الشراء
16. ✅ تقرير المشتريات حسب المورد
17. ✅ تقرير الفواتير المستحقة
18. ✅ إحصائيات لوحة التحكم

---

## 📈 أمثلة الاستخدام | Usage Examples

### 1. إنشاء أمر شراء | Create Purchase Order
```php
use App\Services\PurchaseOrderService;

$service = app(PurchaseOrderService::class);

$order = $service->createOrder([
    'supplier_id' => 1,
    'warehouse_id' => 1,
    'order_date' => now()->toDateString(),
    'expected_date' => now()->addDays(7)->toDateString(),
]);

// إضافة صنف
$service->addItem($order, [
    'item_id' => 1,
    'quantity' => 10,
    'unit_price' => 100,
    'tax_rate' => 15,
    'discount_rate' => 5,
]);

// اعتماد الأمر
$service->approveOrder($order);
```

### 2. استلام البضاعة | Receive Goods
```php
use App\Services\PurchaseReceiptService;

$service = app(PurchaseReceiptService::class);

$receipt = $service->createReceipt([
    'purchase_order_id' => $order->id,
    'supplier_id' => 1,
    'warehouse_id' => 1,
    'receipt_date' => now()->toDateString(),
]);

// إضافة صنف
$service->addItem($receipt, [
    'item_id' => 1,
    'quantity' => 10,
    'unit_price' => 100,
]);

// اعتماد الاستلام (سيتم إنشاء حركة مخزون تلقائياً)
$service->approveReceipt($receipt);
```

### 3. إنشاء فاتورة مورد | Create Purchase Invoice
```php
use App\Services\PurchaseInvoiceService;

$service = app(PurchaseInvoiceService::class);

$invoice = $service->createInvoice([
    'invoice_number' => 'INV-2025-001',
    'supplier_id' => 1,
    'invoice_date' => now()->toDateString(),
]);

// إضافة صنف
$service->addItem($invoice, [
    'item_id' => 1,
    'quantity' => 10,
    'unit_price' => 100,
    'tax_rate' => 15,
]);

// اعتماد الفاتورة (سيتم إنشاء قيد محاسبي تلقائياً)
$service->approveInvoice($invoice);

// تسجيل دفعة
$service->recordPayment($invoice, [
    'amount' => 500,
    'payment_method' => 'cash',
]);
```

---

## 🎨 واجهات المستخدم | User Interfaces

### المسارات الرئيسية | Main Routes
- `/purchases/dashboard` - لوحة التحكم
- `/purchases/suppliers` - إدارة الموردين
- `/purchases/orders` - إدارة أوامر الشراء
- `/purchases/receipts` - إدارة استلام البضاعة
- `/purchases/invoices` - إدارة فواتير الموردين
- `/purchases/reports/*` - التقارير

---

## 🔐 الأمان والصلاحيات | Security & Permissions

جميع المسارات محمية بـ `auth` middleware. يُنصح بإضافة صلاحيات إضافية:

```php
Route::middleware(['auth', 'permission:manage-purchases'])->group(function () {
    // المسارات المحمية
});
```

---

## 📝 ملاحظات التطوير | Development Notes

### منهجية التوازي الذكي | Smart Parallel Methodology
تم تطوير النظام باستخدام منهجية التوازي الذكي:
- **المجموعة 1**: Models + Migrations (تم بالتوازي)
- **المجموعة 2**: Services (تم بالتوازي)
- **المجموعة 3**: Routes + Tests (تم بالتوازي)

### التوافق مع النظام القديم | Backward Compatibility
تم إضافة حقول إضافية في الجداول للتوافق مع النظام القديم:
- `name`, `description`, `is_active` في معظم الجداول
- دعم كامل للحقول القديمة دون التأثير على الوظائف الجديدة

### التكامل التلقائي | Automatic Integration
- **المخازن**: تكامل تلقائي عند اعتماد استلام البضاعة
- **المحاسبة**: تكامل تلقائي عند اعتماد الفواتير والدفعات
- **لا حاجة لتدخل يدوي** في عمليات التكامل

---

## 🚀 التحسينات المستقبلية | Future Enhancements

1. ✨ إضافة واجهات مستخدم (Views) كاملة
2. ✨ تصدير التقارير إلى Excel/PDF
3. ✨ إشعارات تلقائية للفواتير المستحقة
4. ✨ لوحة تحكم تفاعلية مع رسوم بيانية
5. ✨ API RESTful كامل
6. ✨ دعم العملات المتعددة
7. ✨ سجل التغييرات (Audit Log)

---

## 📞 الدعم والمساعدة | Support

للمزيد من المعلومات أو الدعم:
- **الإصدار**: v4.1.0
- **تاريخ الإصدار**: 2025-12-06
- **الفرع**: `feature/purchases-system-v4.1.0`

---

## 📄 الترخيص | License

جميع الحقوق محفوظة © 2025
