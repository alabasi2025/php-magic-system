# 🧬 Factory Generator v3.25.0 - وثيقة التصميم

**تاريخ الإنشاء:** 2025-12-03

**المؤلف:** Manus AI

---

## 1. نظرة عامة

نظام **Factory Generator** هو أداة ذكية تهدف إلى تسريع عملية تطوير تطبيقات Laravel من خلال توليد مصانع البيانات (Factories) تلقائياً. يدعم النظام طرق إدخال متعددة، بما في ذلك الوصف النصي، JSON Schema، القوالب الجاهزة، والهندسة العكسية من الـ Models الموجودة.

هذا النظام يتبع نفس بنية وتصميم المولدات الأخرى في المشروع مثل `MigrationGenerator` و `SeederGenerator` لضمان التناسق وسهولة الصيانة.

## 2. الميزات الرئيسية

- **توليد من وصف نصي:** كتابة وصف بلغة طبيعية (عربي/إنجليزي) لتوليد Factory.
- **توليد من JSON Schema:** تحكم دقيق في بنية الـ Factory من خلال تزويد schema.
- **توليد من قوالب:** استخدام قوالب factories جاهزة وقابلة لإعادة الاستخدام.
- **هندسة عكسية:** توليد factory تلقائياً من خلال تحليل Model موجود وقاعدة البيانات.
- **واجهة مستخدم متكاملة:** واجهة ويب سهلة الاستخدام لإدارة وتوليد الـ Factories.
- **API Endpoints:** توفير نقاط نهاية (Endpoints) للتعامل مع المولد برمجياً.
- **تكامل مع الذكاء الاصطناعي (مستقبلي):** إمكانية استخدام نماذج اللغة الكبيرة لتحليل الوصف النصي بشكل أكثر دقة.

## 3. بنية النظام

يتكون النظام من المكونات التالية:

| المكون | المسار | الوصف |
|---|---|---|
| **Model** | `app/Models/FactoryGeneration.php` | يخزن سجلات عمليات التوليد. |
| **Model** | `app/Models/FactoryTemplate.php` | يخزن قوالب الـ Factories. |
| **Service** | `app/Services/FactoryGeneratorService.php` | يحتوي على المنطق البرمجي لعمليات التوليد. |
| **Controller** | `app/Http/Controllers/FactoryGeneratorController.php` | يعالج طلبات الويب وطلبات الـ API. |
| **Routes** | `routes/factory_generator.php` | يحدد مسارات الويب والـ API الخاصة بالمولد. |
| **Views** | `resources/views/factory-generator/` | يحتوي على واجهات المستخدم (Index, Create, Show). |
| **Migration** | `database/migrations/...` | ملفات الـ Migration لإنشاء الجداول اللازمة. |

## 4. بنية قاعدة البيانات

### جدول `factory_generations`

يستخدم هذا الجدول لتتبع كل عملية توليد factory.

| الحقل | النوع | الوصف |
|---|---|---|
| `id` | `bigint`, `unsigned`, `auto-increment` | المعرف الرئيسي. |
| `name` | `string` | اسم الـ Factory المولد (e.g., `ProductFactory`). |
| `description` | `text`, `nullable` | الوصف النصي المستخدم في التوليد. |
| `model_name` | `string` | اسم الـ Model المرتبط (e.g., `Product`). |
| `table_name` | `string`, `nullable` | اسم الجدول المرتبط. |
| `input_method` | `string` | طريقة الإدخال (`web`, `json`, `template`, `reverse`). |
| `input_data` | `json`, `nullable` | البيانات المدخلة (e.g., JSON Schema). |
| `generated_content` | `longText`, `nullable` | الكود المولد للـ Factory. |
| `file_path` | `string`, `nullable` | مسار الملف بعد الحفظ. |
| `status` | `string` | حالة العملية (`draft`, `generated`, `saved`, `error`). |
| `error_message` | `text`, `nullable` | رسالة الخطأ في حال الفشل. |
| `created_by` | `foreignId`, `nullable` | معرف المستخدم الذي أنشأ السجل. |
| `updated_by` | `foreignId`, `nullable` | معرف المستخدم الذي عدّل السجل. |
| `timestamps` | `timestamps` | أوقات الإنشاء والتعديل. |
| `softDeletes` | `softDeletes` | للحذف الناعم. |

### جدول `factory_templates`

يستخدم هذا الجدول لتخزين قوالب factories قابلة لإعادة الاستخدام.

| الحقل | النوع | الوصف |
|---|---|---|
| `id` | `bigint`, `unsigned`, `auto-increment` | المعرف الرئيسي. |
| `name` | `string` | اسم القالب. |
| `description` | `text`, `nullable` | وصف القالب. |
| `category` | `string` | فئة القالب (e.g., `ecommerce`, `blog`). |
| `model_name` | `string` | اسم الـ Model الافتراضي. |
| `schema` | `json` | بنية الـ Factory على شكل JSON Schema. |
| `is_public` | `boolean` | هل القالب عام أم خاص. |
| `usage_count` | `integer` | عدد مرات استخدام القالب. |
| `rating` | `decimal` | تقييم القالب. |
| `created_by` | `foreignId`, `nullable` | معرف المستخدم الذي أنشأ القالب. |
| `timestamps` | `timestamps` | أوقات الإنشاء والتعديل. |
| `softDeletes` | `softDeletes` | للحذف الناعم. |

## 5. آلية العمل

1.  **اختيار طريقة التوليد:** يختار المستخدم إحدى الطرق المتاحة من واجهة المستخدم (نص، JSON، قالب، Model).
2.  **إرسال الطلب:** يتم إرسال الطلب إلى `FactoryGeneratorController`.
3.  **استدعاء الخدمة:** يقوم الـ Controller باستدعاء الدالة المناسبة في `FactoryGeneratorService`.
4.  **تحليل وتوليد:** تقوم الخدمة بتحليل المدخلات وبناء محتوى الـ Factory باستخدام قوالب نصية.
5.  **تخزين النتيجة:** يتم إنشاء سجل جديد في جدول `factory_generations` وتخزين الكود المولد فيه.
6.  **عرض النتيجة:** يتم توجيه المستخدم إلى صفحة عرض الـ Factory المولد، حيث يمكنه مراجعة الكود وتعديله.
7.  **حفظ كملف:** يمكن للمستخدم حفظ الكود المولد كملف فعلي في المسار `database/factories/`.

## 6. واجهات المستخدم (Views)

-   **`index.blade.php`**: صفحة رئيسية تعرض قائمة بجميع عمليات التوليد السابقة مع إمكانية البحث والفلترة.
-   **`create.blade.php`**: صفحة تحتوي على Tabs لاختيار طريقة التوليد وتوفير المدخلات اللازمة.
-   **`show.blade.php`**: صفحة تعرض تفاصيل عملية التوليد، بما في ذلك الكود المولد، مع إمكانية التعديل، التحميل، والحفظ كملف.

## 7. نقاط النهاية (API Endpoints)

| المسار | الطريقة | الوصف |
|---|---|---|
| `/api/factory-generator` | `GET` | الحصول على قائمة الـ Factories المولدة. |
| `/api/factory-generator/generate` | `POST` | توليد Factory جديد. |
| `/api/factory-generator/{id}` | `GET` | عرض تفاصيل Factory معين. |
| `/api/factory-generator/{id}` | `PUT` | تحديث محتوى Factory معين. |
| `/api/factory-generator/{id}` | `DELETE` | حذف Factory معين. |
| `/api/factory-generator/templates` | `GET` | الحصول على قائمة القوالب المتاحة. |

---


**نهاية الوثيقة**
