# 📚 دليل اختبار الـ APIs الشامل

**الإصدار:** 1.0  
**التاريخ:** 2025-12-02  
**الحالة:** جاهز للاستخدام

---

## 📋 جدول المحتويات

1. [المقدمة](#المقدمة)
2. [المتطلبات](#المتطلبات)
3. [الإعداد الأولي](#الإعداد-الأولي)
4. [اختبار الـ APIs](#اختبار-الـ-apis)
5. [اختبار الأداء](#اختبار-الأداء)
6. [معالجة الأخطاء](#معالجة-الأخطاء)
7. [التقارير والتوثيق](#التقارير-والتوثيق)

---

## 🎯 المقدمة

هذا الدليل يوفر خطوات شاملة لاختبار جميع الـ APIs في نظام المطور المتكامل:

- **Code Generator APIs** - توليد الأكواد
- **Helper Tools APIs** - أدوات الذكاء الاصطناعي المساعدة
- **System Status APIs** - حالة النظام

---

## ✅ المتطلبات

### البرامج المطلوبة:

1. **Postman** (للاختبار اليدوي)
   - تحميل: https://www.postman.com/downloads/
   - الإصدار: 10.0+

2. **cURL** (للاختبار من سطر الأوامر)
   ```bash
   # التحقق من التثبيت
   curl --version
   ```

3. **Apache Bench** (لاختبار الأداء)
   ```bash
   # التحقق من التثبيت
   ab -h
   ```

4. **PHP 8.2+** (لتشغيل الخادم)
   ```bash
   php -v
   ```

5. **Laravel 12+** (الإطار العمل)

### المتطلبات الأخرى:

- OpenAI API Key
- قاعدة بيانات MySQL
- متصفح ويب حديث

---

## 🚀 الإعداد الأولي

### الخطوة 1: تشغيل الخادم

```bash
# انتقل إلى مجلد المشروع
cd /home/ubuntu/php-magic-system

# تشغيل خادم Laravel
php artisan serve

# أو تشغيل على منفذ مختلف
php artisan serve --port=8001
```

### الخطوة 2: إعداد قاعدة البيانات

```bash
# تشغيل الـ Migrations
php artisan migrate

# ملء البيانات الأولية (اختياري)
php artisan db:seed
```

### الخطوة 3: التحقق من الاتصال

```bash
# اختبر الاتصال
curl http://localhost:8000/

# يجب أن تحصل على صفحة Laravel
```

### الخطوة 4: استيراد Postman Collection

1. افتح Postman
2. اذهب إلى: File > Import
3. اختر الملف: `postman_collection.json`
4. اضغط Import

---

## 🧪 اختبار الـ APIs

### الطريقة 1: استخدام Postman

#### اختبار Code Generator:

1. **فتح الـ Collection:**
   - في Postman، اذهب إلى Collections
   - اختر "SEMOP Developer System APIs"

2. **تعيين المتغيرات:**
   - اضغط على Environment
   - عدّل `base_url` إلى `http://localhost:8000`
   - عدّل `csrf_token` بـ CSRF Token من الصفحة

3. **تشغيل الاختبار:**
   - اختر الطلب المطلوب
   - اضغط Send
   - تحقق من النتيجة

#### مثال: توليد CRUD

```
1. اختر: POST /developer/ai/code-generator
2. تحقق من البيانات:
   {
     "description": "نموذج المنتجات...",
     "model_name": "Product",
     "fields": [...],
     "auto_save": false
   }
3. اضغط Send
4. تحقق من الاستجابة
```

### الطريقة 2: استخدام cURL

#### اختبار Code Generator:

```bash
# 1. الحصول على CSRF Token
csrf_token=$(curl -s http://localhost:8000/developer/ai/code-generator | grep -oP 'csrf-token" content="\K[^"]+')

# 2. إرسال الطلب
curl -X POST http://localhost:8000/developer/ai/code-generator \
  -H "Content-Type: application/json" \
  -H "X-CSRF-TOKEN: $csrf_token" \
  -d '{
    "description": "نموذج المنتجات يحتوي على اسم، وصف، سعر",
    "model_name": "Product",
    "fields": ["name", "description", "price"],
    "auto_save": false
  }'
```

#### اختبار Helper Tools:

```bash
# مراجعة الأكواد
curl -X POST http://localhost:8000/developer/ai/code-review \
  -H "Content-Type: application/json" \
  -H "X-CSRF-TOKEN: $csrf_token" \
  -d '{
    "code": "<?php echo \"test\"; ?>",
    "language": "php"
  }'
```

### الطريقة 3: استخدام Laravel Tests

#### تشغيل Unit Tests:

```bash
# تشغيل جميع الاختبارات
php artisan test

# تشغيل اختبار معين
php artisan test tests/Unit/AiCodeGeneratorServiceTest.php

# تشغيل مع التفاصيل
php artisan test --verbose
```

#### تشغيل Feature Tests:

```bash
# تشغيل جميع الاختبارات
php artisan test tests/Feature/

# تشغيل اختبار معين
php artisan test tests/Feature/AiCodeGeneratorApiTest.php
```

---

## ⚡ اختبار الأداء

### اختبار 1: وقت الاستجابة

```bash
# استخدم Apache Bench
ab -n 10 -c 1 http://localhost:8000/system-status

# النتائج المتوقعة:
# - Requests per second: > 10
# - Time per request: < 1000ms
# - Failed requests: 0
```

### اختبار 2: الحمل العالي

```bash
# اختبر مع 100 طلب متزامن
ab -n 100 -c 10 http://localhost:8000/developer/ai/code-generator

# النتائج المتوقعة:
# - معدل النجاح: > 90%
# - لا توجد أخطاء 500
```

### اختبار 3: الاستقرار

```bash
# اختبر لمدة 5 دقائق
ab -t 300 http://localhost:8000/system-status

# النتائج المتوقعة:
# - استقرار في الأداء
# - لا توجد تسريبات ذاكرة
```

---

## ❌ معالجة الأخطاء

### خطأ 1: CSRF Token غير صحيح

**الأعراض:**
```
Error: CSRF token mismatch
Status: 419
```

**الحل:**
```bash
# احصل على CSRF Token جديد
csrf_token=$(curl -s http://localhost:8000/developer/ai/code-generator | grep -oP 'csrf-token" content="\K[^"]+')

# استخدمه في الطلب
curl -X POST http://localhost:8000/developer/ai/code-generator \
  -H "X-CSRF-TOKEN: $csrf_token" \
  ...
```

### خطأ 2: OpenAI API Key غير صحيح

**الأعراض:**
```
Error: Invalid API key
Status: 500
```

**الحل:**
```bash
# تحقق من ملف .env
cat .env | grep OPENAI_API_KEY

# عدّل الـ Key إذا لزم الأمر
# ثم أعد تشغيل الخادم
php artisan serve
```

### خطأ 3: بيانات غير صحيحة

**الأعراض:**
```
Error: Validation failed
Status: 422
```

**الحل:**
```bash
# تحقق من البيانات المرسلة
# تأكد من:
# - description موجود وليس فارغ
# - model_name بصيغة PascalCase
# - fields مصفوفة صحيحة
```

### خطأ 4: خادم غير متاح

**الأعراض:**
```
Error: Connection refused
```

**الحل:**
```bash
# تحقق من تشغيل الخادم
ps aux | grep artisan

# شغّل الخادم إذا لم يكن يعمل
php artisan serve
```

---

## 📊 التقارير والتوثيق

### إنشاء تقرير الاختبار

```bash
# 1. قم بتشغيل جميع الاختبارات
php artisan test --coverage

# 2. سيتم إنشاء تقرير في:
# storage/logs/test-report.html

# 3. افتح التقرير في المتصفح
open storage/logs/test-report.html
```

### توثيق النتائج

**نموذج تقرير:**

```markdown
# تقرير اختبار الـ APIs

**التاريخ:** 2025-12-02  
**المختبر:** [الاسم]  
**الحالة:** ✅ نجح

## النتائج:

### Code Generator APIs:
- ✅ توليد CRUD: نجح
- ✅ توليد Migration: نجح
- ✅ توليد API Resource: نجح
- ✅ توليد Tests: نجح

### Helper Tools APIs:
- ✅ مراجعة الأكواد: نجح
- ✅ إصلاح الأخطاء: نجح
- ✅ توليد الاختبارات: نجح
- ✅ توليد التوثيق: نجح

### System APIs:
- ✅ حالة النظام: نجح
- ✅ الإحصائيات: نجح

## الأداء:

| المقياس | القيمة | الحد المقبول |
|--------|--------|-------------|
| وقت الاستجابة | 500ms | < 1000ms |
| معدل النجاح | 99% | > 95% |
| الأخطاء | 1 | 0 |

## الملاحظات:

- جميع الـ APIs تعمل بشكل صحيح
- الأداء ممتازة
- لا توجد مشاكل حرجة

## التوصيات:

1. تحسين وقت الاستجابة للـ Code Generator
2. إضافة caching للنتائج المتكررة
3. إضافة rate limiting

---

**التوقيع:** _______________  
**التاريخ:** _______________
```

---

## 🔄 دورة الاختبار الكاملة

### الأسبوع الأول:

```
الاثنين:
- اختبار Code Generator APIs
- اختبار الـ Validation

الثلاثاء:
- اختبار Helper Tools APIs
- اختبار معالجة الأخطاء

الأربعاء:
- اختبار System APIs
- اختبار الأداء

الخميس:
- اختبار الحمل العالي
- توثيق النتائج

الجمعة:
- مراجعة النتائج
- إصلاح المشاكل
```

---

## 📝 قائمة التحقق

### قبل الاختبار:

- [ ] الخادم يعمل بشكل صحيح
- [ ] قاعدة البيانات متصلة
- [ ] OpenAI API Key صحيح
- [ ] Postman مثبت ومستورد
- [ ] cURL مثبت

### أثناء الاختبار:

- [ ] جميع الطلبات تعيد 200
- [ ] البيانات المرجعة صحيحة
- [ ] رسائل الخطأ واضحة
- [ ] الأداء مقبولة
- [ ] لا توجد تسريبات ذاكرة

### بعد الاختبار:

- [ ] توثيق جميع النتائج
- [ ] إصلاح المشاكل المكتشفة
- [ ] إعادة الاختبار للمشاكل المصلحة
- [ ] إنشاء تقرير نهائي
- [ ] تحديث التوثيق

---

## 🎓 أفضل الممارسات

### 1. التنظيم:

```
- استخدم Postman Collections لتنظيم الطلبات
- استخدم Environments لإدارة المتغيرات
- استخدم Folders لتنظيم الاختبارات
```

### 2. التوثيق:

```
- وثّق كل اختبار بوضوح
- أضف تعليقات على الطلبات
- احفظ النتائج في ملفات
```

### 3. الأتمتة:

```
- استخدم Laravel Tests للأتمتة
- استخدم CI/CD Pipeline
- شغّل الاختبارات تلقائياً
```

### 4. المراقبة:

```
- راقب الأداء بشكل مستمر
- احفظ السجلات
- أنشئ تنبيهات للأخطاء
```

---

## 📞 الدعم والمساعدة

### للمزيد من المعلومات:

- 📖 اقرأ التوثيق الكاملة
- 🚀 اتبع دليل البدء السريع
- 💬 تواصل مع فريق الدعم

### الأسئلة الشائعة:

**س:** كيف أحصل على CSRF Token؟  
**ج:** استخدم cURL للحصول على الصفحة وابحث عن `csrf-token`

**س:** هل يمكن استخدام API بدون CSRF Token؟  
**ج:** نعم، إذا كنت تستخدم API Token أو Authorization Header

**س:** كم وقت يستغرق توليد CRUD؟  
**ج:** عادة 5-10 ثواني حسب تعقيد الوصف

**س:** هل الأكواد المولدة آمنة للإنتاج؟  
**ج:** نعم، لكن يجب مراجعتها قبل الاستخدام

---

## 📊 ملخص الاختبارات

| الفئة | العدد | الحالة |
|------|------|--------|
| Code Generator APIs | 4 | ✅ |
| Helper Tools APIs | 4 | ✅ |
| System APIs | 2 | ✅ |
| Error Handling | 4 | ✅ |
| Performance | 3 | ✅ |
| **الإجمالي** | **17** | **✅** |

---

**تاريخ الإنشاء:** 2025-12-02  
**آخر تحديث:** 2025-12-02  
**الإصدار:** 1.0

---

*شكراً لاستخدامك نظام المطور المتكامل!* 🚀
