# 🛡️ Middleware Templates Guide

## نظرة عامة

هذا المجلد يحتوي على قوالب Middleware جاهزة للاستخدام مع Middleware Generator v3.28.0.

## القوالب المتاحة

### 1. API Versioning Template
**الملف:** `api-versioning.php`

قالب لإدارة إصدارات API بشكل احترافي.

**الميزات:**
- دعم إصدارات متعددة (v1, v2, v3)
- قراءة الإصدار من Header أو URL
- التحقق من صحة الإصدار
- إضافة رأس الإصدار للاستجابة

**الاستخدام:**
```bash
php artisan generate:middleware --template=api-versioning --name=ApiVersioningMiddleware
```

## JSON Schema

### مثال Schema
**الملف:** `example-schema.json`

```json
{
  "name": "CustomApiMiddleware",
  "type": "auth",
  "description": "Custom API authentication middleware",
  "options": {
    "author": "SEMOP Team",
    "version": "1.0.0"
  }
}
```

**الاستخدام:**
```bash
php artisan generate:middleware --json=app/Templates/Middleware/example-schema.json
```

## إنشاء قوالب مخصصة

### خطوات إنشاء قالب جديد:

1. **إنشاء ملف PHP** في هذا المجلد
2. **استخدام المتغيرات** التالية:
   - `{{namespace}}` - Namespace الخاص بـ Middleware
   - `{{name}}` - اسم الـ Middleware
   - `{{description}}` - وصف الـ Middleware
   - `{{version}}` - رقم الإصدار
   - `{{date}}` - تاريخ الإنشاء
   - `{{author}}` - اسم المؤلف

3. **مثال على قالب بسيط:**

```php
<?php

namespace {{namespace}};

use Closure;
use Illuminate\Http\Request;

/**
 * {{name}}
 * 
 * {{description}}
 * 
 * @version {{version}}
 * @date {{date}}
 * @author {{author}}
 */
class {{name}}
{
    public function handle(Request $request, Closure $next)
    {
        // Your logic here
        
        return $next($request);
    }
}
```

## المتغيرات المتاحة

| المتغير | الوصف | مثال |
|---------|-------|------|
| `{{namespace}}` | Namespace الكلاس | `App\Http\Middleware` |
| `{{name}}` | اسم الكلاس | `CustomMiddleware` |
| `{{description}}` | وصف الـ Middleware | `Custom middleware for...` |
| `{{version}}` | رقم الإصدار | `3.28.0` |
| `{{date}}` | تاريخ الإنشاء | `2025-12-03` |
| `{{author}}` | اسم المؤلف | `Manus AI` |

## أمثلة الاستخدام

### 1. توليد من قالب
```bash
php artisan generate:middleware --template=api-versioning --name=MyVersioningMiddleware
```

### 2. توليد من JSON
```bash
php artisan generate:middleware --json=path/to/schema.json
```

### 3. توليد من وصف نصي
```bash
php artisan generate:middleware --text="middleware for API rate limiting"
```

### 4. توليد وحفظ مباشرة
```bash
php artisan generate:middleware --text="authentication middleware" --save
```

## نصائح

1. **استخدم أسماء واضحة** للقوالب تعبر عن وظيفتها
2. **أضف تعليقات توضيحية** داخل القوالب
3. **اختبر القوالب** قبل استخدامها في الإنتاج
4. **وثق القوالب المخصصة** في هذا الملف

## الدعم

للمزيد من المعلومات، راجع:
- [Middleware Generator Documentation](../../docs/middleware-generator.md)
- [Laravel Middleware Documentation](https://laravel.com/docs/middleware)

---

**Generated by Middleware Generator v3.28.0**  
**SEMOP Team © 2025**
