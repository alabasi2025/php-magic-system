<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Models\ManusTransaction;
use Illuminate\Http\Request;
use Illuminate\Http\JsonResponse;

/**
 * ManusTransactionApiController
 * 
 * RESTful API Controller for ManusTransaction
 * Auto-generated by API Generator v3.16.0
 * 
 * @version 3.16.0
 * @date 2025-12-03
 */
class ManusTransactionApiController extends Controller
{
    /**
     * Display a listing of the resource.
     * 
     * @OA\Get(
     *     path="/api/manus-transactions",
     *     summary="Get all ManusTransaction records",
     *     tags={"ManusTransaction"},
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function index(Request $request): JsonResponse
    {
        try {
            $perPage = $request->get('per_page', 15);
            $page = $request->get('page', 1);
            
            $manusTransactions = ManusTransaction::query()
                ->when($request->has('search'), function ($query) use ($request) {
                    $search = $request->get('search');
                    // Add searchable fields here
                    return $query->where('id', 'like', "%{$search}%");
                })
                ->paginate($perPage, ['*'], 'page', $page);

            return response()->json([
                'success' => true,
                'data' => $manusTransactions->items(),
                'meta' => [
                    'current_page' => $manusTransactions->currentPage(),
                    'per_page' => $manusTransactions->perPage(),
                    'total' => $manusTransactions->total(),
                    'last_page' => $manusTransactions->lastPage(),
                ]
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to retrieve records',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Store a newly created resource in storage.
     * 
     * @OA\Post(
     *     path="/api/manus-transactions",
     *     summary="Create a new ManusTransaction",
     *     tags={"ManusTransaction"},
     *     @OA\Response(response=201, description="Created")
     * )
     */
    public function store(Request $request): JsonResponse
    {
        try {
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $manusTransaction = ManusTransaction::create($validated);

            return response()->json([
                'success' => true,
                'message' => 'ManusTransaction created successfully',
                'data' => $manusTransaction
            ], 201);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to create ManusTransaction',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Display the specified resource.
     * 
     * @OA\Get(
     *     path="/api/manus-transactions/{id}",
     *     summary="Get a specific ManusTransaction",
     *     tags={"ManusTransaction"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function show($id): JsonResponse
    {
        try {
            $manusTransaction = ManusTransaction::findOrFail($id);

            return response()->json([
                'success' => true,
                'data' => $manusTransaction
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'ManusTransaction not found',
                'error' => $e->getMessage()
            ], 404);
        }
    }

    /**
     * Update the specified resource in storage.
     * 
     * @OA\Put(
     *     path="/api/manus-transactions/{id}",
     *     summary="Update a ManusTransaction",
     *     tags={"ManusTransaction"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function update(Request $request, $id): JsonResponse
    {
        try {
            $manusTransaction = ManusTransaction::findOrFail($id);
            
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $manusTransaction->update($validated);

            return response()->json([
                'success' => true,
                'message' => 'ManusTransaction updated successfully',
                'data' => $manusTransaction
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to update ManusTransaction',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Remove the specified resource from storage.
     * 
     * @OA\Delete(
     *     path="/api/manus-transactions/{id}",
     *     summary="Delete a ManusTransaction",
     *     tags={"ManusTransaction"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function destroy($id): JsonResponse
    {
        try {
            $manusTransaction = ManusTransaction::findOrFail($id);
            $manusTransaction->delete();

            return response()->json([
                'success' => true,
                'message' => 'ManusTransaction deleted successfully'
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to delete ManusTransaction',
                'error' => $e->getMessage()
            ], 500);
        }
    }
}
