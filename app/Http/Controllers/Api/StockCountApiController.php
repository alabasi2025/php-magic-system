<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Models\StockCount;
use Illuminate\Http\Request;
use Illuminate\Http\JsonResponse;

/**
 * StockCountApiController
 * 
 * RESTful API Controller for StockCount
 * Auto-generated by API Generator v3.16.0
 * 
 * @version 3.16.0
 * @date 2025-12-03
 */
class StockCountApiController extends Controller
{
    /**
     * Display a listing of the resource.
     * 
     * @OA\Get(
     *     path="/api/stock-counts",
     *     summary="Get all StockCount records",
     *     tags={"StockCount"},
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function index(Request $request): JsonResponse
    {
        try {
            $perPage = $request->get('per_page', 15);
            $page = $request->get('page', 1);
            
            $stockCounts = StockCount::query()
                ->when($request->has('search'), function ($query) use ($request) {
                    $search = $request->get('search');
                    // Add searchable fields here
                    return $query->where('id', 'like', "%{$search}%");
                })
                ->paginate($perPage, ['*'], 'page', $page);

            return response()->json([
                'success' => true,
                'data' => $stockCounts->items(),
                'meta' => [
                    'current_page' => $stockCounts->currentPage(),
                    'per_page' => $stockCounts->perPage(),
                    'total' => $stockCounts->total(),
                    'last_page' => $stockCounts->lastPage(),
                ]
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to retrieve records',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Store a newly created resource in storage.
     * 
     * @OA\Post(
     *     path="/api/stock-counts",
     *     summary="Create a new StockCount",
     *     tags={"StockCount"},
     *     @OA\Response(response=201, description="Created")
     * )
     */
    public function store(Request $request): JsonResponse
    {
        try {
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $stockCount = StockCount::create($validated);

            return response()->json([
                'success' => true,
                'message' => 'StockCount created successfully',
                'data' => $stockCount
            ], 201);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to create StockCount',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Display the specified resource.
     * 
     * @OA\Get(
     *     path="/api/stock-counts/{id}",
     *     summary="Get a specific StockCount",
     *     tags={"StockCount"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function show($id): JsonResponse
    {
        try {
            $stockCount = StockCount::findOrFail($id);

            return response()->json([
                'success' => true,
                'data' => $stockCount
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'StockCount not found',
                'error' => $e->getMessage()
            ], 404);
        }
    }

    /**
     * Update the specified resource in storage.
     * 
     * @OA\Put(
     *     path="/api/stock-counts/{id}",
     *     summary="Update a StockCount",
     *     tags={"StockCount"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function update(Request $request, $id): JsonResponse
    {
        try {
            $stockCount = StockCount::findOrFail($id);
            
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $stockCount->update($validated);

            return response()->json([
                'success' => true,
                'message' => 'StockCount updated successfully',
                'data' => $stockCount
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to update StockCount',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Remove the specified resource from storage.
     * 
     * @OA\Delete(
     *     path="/api/stock-counts/{id}",
     *     summary="Delete a StockCount",
     *     tags={"StockCount"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function destroy($id): JsonResponse
    {
        try {
            $stockCount = StockCount::findOrFail($id);
            $stockCount->delete();

            return response()->json([
                'success' => true,
                'message' => 'StockCount deleted successfully'
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to delete StockCount',
                'error' => $e->getMessage()
            ], 500);
        }
    }
}
