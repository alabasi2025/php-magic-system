<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Models\StockMovement;
use Illuminate\Http\Request;
use Illuminate\Http\JsonResponse;

/**
 * StockMovementApiController
 * 
 * RESTful API Controller for StockMovement
 * Auto-generated by API Generator v3.16.0
 * 
 * @version 3.16.0
 * @date 2025-12-03
 */
class StockMovementApiController extends Controller
{
    /**
     * Display a listing of the resource.
     * 
     * @OA\Get(
     *     path="/api/stock-movements",
     *     summary="Get all StockMovement records",
     *     tags={"StockMovement"},
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function index(Request $request): JsonResponse
    {
        try {
            $perPage = $request->get('per_page', 15);
            $page = $request->get('page', 1);
            
            $stockMovements = StockMovement::query()
                ->when($request->has('search'), function ($query) use ($request) {
                    $search = $request->get('search');
                    // Add searchable fields here
                    return $query->where('id', 'like', "%{$search}%");
                })
                ->paginate($perPage, ['*'], 'page', $page);

            return response()->json([
                'success' => true,
                'data' => $stockMovements->items(),
                'meta' => [
                    'current_page' => $stockMovements->currentPage(),
                    'per_page' => $stockMovements->perPage(),
                    'total' => $stockMovements->total(),
                    'last_page' => $stockMovements->lastPage(),
                ]
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to retrieve records',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Store a newly created resource in storage.
     * 
     * @OA\Post(
     *     path="/api/stock-movements",
     *     summary="Create a new StockMovement",
     *     tags={"StockMovement"},
     *     @OA\Response(response=201, description="Created")
     * )
     */
    public function store(Request $request): JsonResponse
    {
        try {
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $stockMovement = StockMovement::create($validated);

            return response()->json([
                'success' => true,
                'message' => 'StockMovement created successfully',
                'data' => $stockMovement
            ], 201);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to create StockMovement',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Display the specified resource.
     * 
     * @OA\Get(
     *     path="/api/stock-movements/{id}",
     *     summary="Get a specific StockMovement",
     *     tags={"StockMovement"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function show($id): JsonResponse
    {
        try {
            $stockMovement = StockMovement::findOrFail($id);

            return response()->json([
                'success' => true,
                'data' => $stockMovement
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'StockMovement not found',
                'error' => $e->getMessage()
            ], 404);
        }
    }

    /**
     * Update the specified resource in storage.
     * 
     * @OA\Put(
     *     path="/api/stock-movements/{id}",
     *     summary="Update a StockMovement",
     *     tags={"StockMovement"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function update(Request $request, $id): JsonResponse
    {
        try {
            $stockMovement = StockMovement::findOrFail($id);
            
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $stockMovement->update($validated);

            return response()->json([
                'success' => true,
                'message' => 'StockMovement updated successfully',
                'data' => $stockMovement
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to update StockMovement',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Remove the specified resource from storage.
     * 
     * @OA\Delete(
     *     path="/api/stock-movements/{id}",
     *     summary="Delete a StockMovement",
     *     tags={"StockMovement"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function destroy($id): JsonResponse
    {
        try {
            $stockMovement = StockMovement::findOrFail($id);
            $stockMovement->delete();

            return response()->json([
                'success' => true,
                'message' => 'StockMovement deleted successfully'
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to delete StockMovement',
                'error' => $e->getMessage()
            ], 500);
        }
    }
}
