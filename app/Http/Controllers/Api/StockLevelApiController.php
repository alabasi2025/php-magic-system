<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Models\StockLevel;
use Illuminate\Http\Request;
use Illuminate\Http\JsonResponse;

/**
 * StockLevelApiController
 * 
 * RESTful API Controller for StockLevel
 * Auto-generated by API Generator v3.16.0
 * 
 * @version 3.16.0
 * @date 2025-12-03
 */
class StockLevelApiController extends Controller
{
    /**
     * Display a listing of the resource.
     * 
     * @OA\Get(
     *     path="/api/stock-levels",
     *     summary="Get all StockLevel records",
     *     tags={"StockLevel"},
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function index(Request $request): JsonResponse
    {
        try {
            $perPage = $request->get('per_page', 15);
            $page = $request->get('page', 1);
            
            $stockLevels = StockLevel::query()
                ->when($request->has('search'), function ($query) use ($request) {
                    $search = $request->get('search');
                    // Add searchable fields here
                    return $query->where('id', 'like', "%{$search}%");
                })
                ->paginate($perPage, ['*'], 'page', $page);

            return response()->json([
                'success' => true,
                'data' => $stockLevels->items(),
                'meta' => [
                    'current_page' => $stockLevels->currentPage(),
                    'per_page' => $stockLevels->perPage(),
                    'total' => $stockLevels->total(),
                    'last_page' => $stockLevels->lastPage(),
                ]
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to retrieve records',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Store a newly created resource in storage.
     * 
     * @OA\Post(
     *     path="/api/stock-levels",
     *     summary="Create a new StockLevel",
     *     tags={"StockLevel"},
     *     @OA\Response(response=201, description="Created")
     * )
     */
    public function store(Request $request): JsonResponse
    {
        try {
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $stockLevel = StockLevel::create($validated);

            return response()->json([
                'success' => true,
                'message' => 'StockLevel created successfully',
                'data' => $stockLevel
            ], 201);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to create StockLevel',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Display the specified resource.
     * 
     * @OA\Get(
     *     path="/api/stock-levels/{id}",
     *     summary="Get a specific StockLevel",
     *     tags={"StockLevel"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function show($id): JsonResponse
    {
        try {
            $stockLevel = StockLevel::findOrFail($id);

            return response()->json([
                'success' => true,
                'data' => $stockLevel
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'StockLevel not found',
                'error' => $e->getMessage()
            ], 404);
        }
    }

    /**
     * Update the specified resource in storage.
     * 
     * @OA\Put(
     *     path="/api/stock-levels/{id}",
     *     summary="Update a StockLevel",
     *     tags={"StockLevel"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function update(Request $request, $id): JsonResponse
    {
        try {
            $stockLevel = StockLevel::findOrFail($id);
            
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $stockLevel->update($validated);

            return response()->json([
                'success' => true,
                'message' => 'StockLevel updated successfully',
                'data' => $stockLevel
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to update StockLevel',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Remove the specified resource from storage.
     * 
     * @OA\Delete(
     *     path="/api/stock-levels/{id}",
     *     summary="Delete a StockLevel",
     *     tags={"StockLevel"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function destroy($id): JsonResponse
    {
        try {
            $stockLevel = StockLevel::findOrFail($id);
            $stockLevel->delete();

            return response()->json([
                'success' => true,
                'message' => 'StockLevel deleted successfully'
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to delete StockLevel',
                'error' => $e->getMessage()
            ], 500);
        }
    }
}
