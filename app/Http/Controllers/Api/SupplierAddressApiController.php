<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Models\SupplierAddress;
use Illuminate\Http\Request;
use Illuminate\Http\JsonResponse;

/**
 * SupplierAddressApiController
 * 
 * RESTful API Controller for SupplierAddress
 * Auto-generated by API Generator v3.16.0
 * 
 * @version 3.16.0
 * @date 2025-12-03
 */
class SupplierAddressApiController extends Controller
{
    /**
     * Display a listing of the resource.
     * 
     * @OA\Get(
     *     path="/api/supplier-addresses",
     *     summary="Get all SupplierAddress records",
     *     tags={"SupplierAddress"},
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function index(Request $request): JsonResponse
    {
        try {
            $perPage = $request->get('per_page', 15);
            $page = $request->get('page', 1);
            
            $supplierAddresses = SupplierAddress::query()
                ->when($request->has('search'), function ($query) use ($request) {
                    $search = $request->get('search');
                    // Add searchable fields here
                    return $query->where('id', 'like', "%{$search}%");
                })
                ->paginate($perPage, ['*'], 'page', $page);

            return response()->json([
                'success' => true,
                'data' => $supplierAddresses->items(),
                'meta' => [
                    'current_page' => $supplierAddresses->currentPage(),
                    'per_page' => $supplierAddresses->perPage(),
                    'total' => $supplierAddresses->total(),
                    'last_page' => $supplierAddresses->lastPage(),
                ]
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to retrieve records',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Store a newly created resource in storage.
     * 
     * @OA\Post(
     *     path="/api/supplier-addresses",
     *     summary="Create a new SupplierAddress",
     *     tags={"SupplierAddress"},
     *     @OA\Response(response=201, description="Created")
     * )
     */
    public function store(Request $request): JsonResponse
    {
        try {
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $supplierAddress = SupplierAddress::create($validated);

            return response()->json([
                'success' => true,
                'message' => 'SupplierAddress created successfully',
                'data' => $supplierAddress
            ], 201);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to create SupplierAddress',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Display the specified resource.
     * 
     * @OA\Get(
     *     path="/api/supplier-addresses/{id}",
     *     summary="Get a specific SupplierAddress",
     *     tags={"SupplierAddress"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function show($id): JsonResponse
    {
        try {
            $supplierAddress = SupplierAddress::findOrFail($id);

            return response()->json([
                'success' => true,
                'data' => $supplierAddress
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'SupplierAddress not found',
                'error' => $e->getMessage()
            ], 404);
        }
    }

    /**
     * Update the specified resource in storage.
     * 
     * @OA\Put(
     *     path="/api/supplier-addresses/{id}",
     *     summary="Update a SupplierAddress",
     *     tags={"SupplierAddress"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function update(Request $request, $id): JsonResponse
    {
        try {
            $supplierAddress = SupplierAddress::findOrFail($id);
            
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $supplierAddress->update($validated);

            return response()->json([
                'success' => true,
                'message' => 'SupplierAddress updated successfully',
                'data' => $supplierAddress
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to update SupplierAddress',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Remove the specified resource from storage.
     * 
     * @OA\Delete(
     *     path="/api/supplier-addresses/{id}",
     *     summary="Delete a SupplierAddress",
     *     tags={"SupplierAddress"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function destroy($id): JsonResponse
    {
        try {
            $supplierAddress = SupplierAddress::findOrFail($id);
            $supplierAddress->delete();

            return response()->json([
                'success' => true,
                'message' => 'SupplierAddress deleted successfully'
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to delete SupplierAddress',
                'error' => $e->getMessage()
            ], 500);
        }
    }
}
