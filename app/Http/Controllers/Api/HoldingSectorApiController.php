<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Models\HoldingSector;
use Illuminate\Http\Request;
use Illuminate\Http\JsonResponse;

/**
 * HoldingSectorApiController
 * 
 * RESTful API Controller for HoldingSector
 * Auto-generated by API Generator v3.16.0
 * 
 * @version 3.16.0
 * @date 2025-12-03
 */
class HoldingSectorApiController extends Controller
{
    /**
     * Display a listing of the resource.
     * 
     * @OA\Get(
     *     path="/api/holding-sectors",
     *     summary="Get all HoldingSector records",
     *     tags={"HoldingSector"},
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function index(Request $request): JsonResponse
    {
        try {
            $perPage = $request->get('per_page', 15);
            $page = $request->get('page', 1);
            
            $holdingSectors = HoldingSector::query()
                ->when($request->has('search'), function ($query) use ($request) {
                    $search = $request->get('search');
                    // Add searchable fields here
                    return $query->where('id', 'like', "%{$search}%");
                })
                ->paginate($perPage, ['*'], 'page', $page);

            return response()->json([
                'success' => true,
                'data' => $holdingSectors->items(),
                'meta' => [
                    'current_page' => $holdingSectors->currentPage(),
                    'per_page' => $holdingSectors->perPage(),
                    'total' => $holdingSectors->total(),
                    'last_page' => $holdingSectors->lastPage(),
                ]
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to retrieve records',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Store a newly created resource in storage.
     * 
     * @OA\Post(
     *     path="/api/holding-sectors",
     *     summary="Create a new HoldingSector",
     *     tags={"HoldingSector"},
     *     @OA\Response(response=201, description="Created")
     * )
     */
    public function store(Request $request): JsonResponse
    {
        try {
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $holdingSector = HoldingSector::create($validated);

            return response()->json([
                'success' => true,
                'message' => 'HoldingSector created successfully',
                'data' => $holdingSector
            ], 201);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to create HoldingSector',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Display the specified resource.
     * 
     * @OA\Get(
     *     path="/api/holding-sectors/{id}",
     *     summary="Get a specific HoldingSector",
     *     tags={"HoldingSector"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function show($id): JsonResponse
    {
        try {
            $holdingSector = HoldingSector::findOrFail($id);

            return response()->json([
                'success' => true,
                'data' => $holdingSector
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'HoldingSector not found',
                'error' => $e->getMessage()
            ], 404);
        }
    }

    /**
     * Update the specified resource in storage.
     * 
     * @OA\Put(
     *     path="/api/holding-sectors/{id}",
     *     summary="Update a HoldingSector",
     *     tags={"HoldingSector"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function update(Request $request, $id): JsonResponse
    {
        try {
            $holdingSector = HoldingSector::findOrFail($id);
            
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $holdingSector->update($validated);

            return response()->json([
                'success' => true,
                'message' => 'HoldingSector updated successfully',
                'data' => $holdingSector
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to update HoldingSector',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Remove the specified resource from storage.
     * 
     * @OA\Delete(
     *     path="/api/holding-sectors/{id}",
     *     summary="Delete a HoldingSector",
     *     tags={"HoldingSector"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function destroy($id): JsonResponse
    {
        try {
            $holdingSector = HoldingSector::findOrFail($id);
            $holdingSector->delete();

            return response()->json([
                'success' => true,
                'message' => 'HoldingSector deleted successfully'
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to delete HoldingSector',
                'error' => $e->getMessage()
            ], 500);
        }
    }
}
