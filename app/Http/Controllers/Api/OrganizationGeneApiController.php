<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Models\OrganizationGene;
use Illuminate\Http\Request;
use Illuminate\Http\JsonResponse;

/**
 * OrganizationGeneApiController
 * 
 * RESTful API Controller for OrganizationGene
 * Auto-generated by API Generator v3.16.0
 * 
 * @version 3.16.0
 * @date 2025-12-03
 */
class OrganizationGeneApiController extends Controller
{
    /**
     * Display a listing of the resource.
     * 
     * @OA\Get(
     *     path="/api/organization-genes",
     *     summary="Get all OrganizationGene records",
     *     tags={"OrganizationGene"},
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function index(Request $request): JsonResponse
    {
        try {
            $perPage = $request->get('per_page', 15);
            $page = $request->get('page', 1);
            
            $organizationGenes = OrganizationGene::query()
                ->when($request->has('search'), function ($query) use ($request) {
                    $search = $request->get('search');
                    // Add searchable fields here
                    return $query->where('id', 'like', "%{$search}%");
                })
                ->paginate($perPage, ['*'], 'page', $page);

            return response()->json([
                'success' => true,
                'data' => $organizationGenes->items(),
                'meta' => [
                    'current_page' => $organizationGenes->currentPage(),
                    'per_page' => $organizationGenes->perPage(),
                    'total' => $organizationGenes->total(),
                    'last_page' => $organizationGenes->lastPage(),
                ]
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to retrieve records',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Store a newly created resource in storage.
     * 
     * @OA\Post(
     *     path="/api/organization-genes",
     *     summary="Create a new OrganizationGene",
     *     tags={"OrganizationGene"},
     *     @OA\Response(response=201, description="Created")
     * )
     */
    public function store(Request $request): JsonResponse
    {
        try {
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $organizationGene = OrganizationGene::create($validated);

            return response()->json([
                'success' => true,
                'message' => 'OrganizationGene created successfully',
                'data' => $organizationGene
            ], 201);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to create OrganizationGene',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Display the specified resource.
     * 
     * @OA\Get(
     *     path="/api/organization-genes/{id}",
     *     summary="Get a specific OrganizationGene",
     *     tags={"OrganizationGene"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function show($id): JsonResponse
    {
        try {
            $organizationGene = OrganizationGene::findOrFail($id);

            return response()->json([
                'success' => true,
                'data' => $organizationGene
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'OrganizationGene not found',
                'error' => $e->getMessage()
            ], 404);
        }
    }

    /**
     * Update the specified resource in storage.
     * 
     * @OA\Put(
     *     path="/api/organization-genes/{id}",
     *     summary="Update a OrganizationGene",
     *     tags={"OrganizationGene"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function update(Request $request, $id): JsonResponse
    {
        try {
            $organizationGene = OrganizationGene::findOrFail($id);
            
            $validated = $request->validate([
                // Add validation rules here
            ]);

            $organizationGene->update($validated);

            return response()->json([
                'success' => true,
                'message' => 'OrganizationGene updated successfully',
                'data' => $organizationGene
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to update OrganizationGene',
                'error' => $e->getMessage()
            ], 500);
        }
    }

    /**
     * Remove the specified resource from storage.
     * 
     * @OA\Delete(
     *     path="/api/organization-genes/{id}",
     *     summary="Delete a OrganizationGene",
     *     tags={"OrganizationGene"},
     *     @OA\Parameter(name="id", in="path", required=true, @OA\Schema(type="integer")),
     *     @OA\Response(response=200, description="Success")
     * )
     */
    public function destroy($id): JsonResponse
    {
        try {
            $organizationGene = OrganizationGene::findOrFail($id);
            $organizationGene->delete();

            return response()->json([
                'success' => true,
                'message' => 'OrganizationGene deleted successfully'
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'success' => false,
                'message' => 'Failed to delete OrganizationGene',
                'error' => $e->getMessage()
            ], 500);
        }
    }
}
