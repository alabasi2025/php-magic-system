> 🏭 **Factory Generator v3.25.0 - دليل المستخدم**

**تاريخ التحديث:** 2025-12-03

**المؤلف:** Manus AI

---

## 🚀 مقدمة

مرحباً بك في دليل المستخدم الخاص بـ **Factory Generator**، الأداة الذكية التي تمكنك من إنشاء مصانع بيانات (Factories) لتطبيقات Laravel بسهولة وسرعة. سواء كنت تريد إنشاء factory من وصف نصي بسيط أو من بنية JSON معقدة، فإن هذا المولد يغطي جميع احتياجاتك.

## ✨ الميزات

- **طرق إدخال متعددة:** نص، JSON، قوالب جاهزة، وهندسة عكسية من الـ Models.
- **واجهة رسومية سهلة:** إدارة وتوليد الـ Factories من خلال واجهة ويب متكاملة.
- **تخصيص كامل:** إمكانية تعديل الكود المولد قبل حفظه.
- **API متكامل:** أتمتة عمليات التوليد من خلال API.

## 🛠️ كيفية الاستخدام

### 1. الوصول إلى المولد

يمكنك الوصول إلى واجهة المولد من خلال الرابط التالي في لوحة تحكم المطور:

```
/factory-generator
```

ستظهر لك الصفحة الرئيسية التي تعرض جميع الـ Factories التي تم توليدها مسبقاً.

### 2. إنشاء Factory جديد

اضغط على زر **"➕ إنشاء Factory جديد"** للانتقال إلى صفحة الإنشاء. ستجد أربعة خيارات للتوليد:

#### أ. من وصف نصي (الطريقة الأسهل)

1.  اذهب إلى تبويب **"📝 من وصف نصي"**.
2.  اكتب وصفاً للـ Factory الذي تريده في حقل "الوصف النصي". كن واضحاً قدر الإمكان.

    **مثال:**

    ```
    أريد factory لموديل Product
    يحتوي على:
    - اسم المنتج (name)
    - السعر (price)
    - وصف (description)
    - صورة (image)
    ```

3.  اضغط على **"🚀 توليد Factory"**.

#### ب. من JSON Schema (للتحكم الدقيق)

1.  اذهب إلى تبويب **"📋 من JSON Schema"**.
2.  أدخل بنية الـ Factory على شكل JSON.

    **مثال:**

    ```json
    {
      "model_name": "Product",
      "fields": {
        "name": {
          "faker": "commerce.productName()"
        },
        "price": {
          "faker": "commerce.price(100, 5000)"
        },
        "description": {
          "faker": "text(250)"
        }
      }
    }
    ```

3.  اضغط على **"🚀 توليد Factory"**.

#### ج. من قالب جاهز

1.  اذهب إلى تبويب **"📦 من قالب جاهز"**.
2.  اختر القالب المناسب من القائمة.
3.  أدخل اسم الـ **Model** المطلوب.
4.  اضغط على **"🚀 توليد Factory"**.

#### د. من Model موجود (هندسة عكسية)

1.  اذهب إلى تبويب **"🔄 من Model موجود"**.
2.  أدخل اسم الـ **Model** الموجود في مشروعك (e.g., `User`).
3.  سيقوم النظام بتحليل أعمدة الجدول المرتبط بالـ Model وتوليد factory بناءً عليها.
4.  اضغط على **"🚀 توليد Factory"**.

### 3. مراجعة وتعديل الكود

بعد التوليد، سيتم توجيهك إلى صفحة عرض الـ Factory. في هذه الصفحة، يمكنك:

-   **مراجعة الكود:** يتم عرض الكود المولد مع تمييز الألوان (Syntax Highlighting).
-   **تعديل الكود:** اضغط على زر **"✏️ تعديل"** لتتمكن من تحرير الكود مباشرة في المتصفح.
-   **حفظ التعديلات:** بعد التعديل، اضغط على **"💾 حفظ التعديلات"**.

### 4. حفظ واستخدام الـ Factory

1.  بعد التأكد من صحة الكود، اضغط على زر **"💾 حفظ كملف"**.
2.  سيتم حفظ الملف في المسار `database/factories/`.
3.  الآن يمكنك استخدام الـ Factory في الـ Seeders أو الاختبارات الخاصة بك.

    **مثال للاستخدام في Seeder:**

    ```php
    \App\Models\Product::factory()->count(50)->create();
    ```

## ⚙️ واجهة برمجة التطبيقات (API)

يمكنك التفاعل مع المولد برمجياً من خلال الـ API.

-   **`GET /api/factory-generator`**: عرض قائمة الـ Factories.
-   **`POST /api/factory-generator/generate`**: توليد factory جديد.
-   **`GET /api/factory-generator/{id}`**: عرض تفاصيل factory.

**مثال على طلب `POST` لتوليد factory:**

```bash
curl -X POST /api/factory-generator/generate \
-H "Content-Type: application/json" \
-d '{
  "method": "json",
  "data": {
    "model_name": "ApiProduct",
    "fields": {
      "name": { "faker": "name" },
      "price": { "faker": "randomFloat(2, 10, 100)" }
    }
  }
}'
```

---

**نهاية الدليل**
