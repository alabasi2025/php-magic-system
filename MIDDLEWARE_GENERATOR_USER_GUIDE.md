# 🛡️ Middleware Generator v3.28.0 - دليل المستخدم

**التاريخ:** 2025-12-03  
**الإصدار:** 3.28.0  
**المؤلف:** Manus AI  
**المشروع:** php-magic-system (SEMOP)

---

## 1. 🚀 مقدمة

مرحباً بك في دليل المستخدم لـ **Middleware Generator v3.28.0**. هذه الأداة القوية مصممة لتسريع عملية تطوير تطبيقات Laravel من خلال توفير واجهة سطر أوامر (CLI) ذكية لتوليد أنواع مختلفة من Middleware بشكل تلقائي.

سواء كنت تحتاج إلى middleware للمصادقة، أو تحديد معدل الطلبات، أو أي نوع آخر، فإن هذا المولد سيوفر عليك الوقت والجهد، مع ضمان جودة الكود واتباع أفضل الممارسات.

---

## 2. 🎯 الميزات الرئيسية

*   **10 أنواع مدعومة:** يدعم المولد 10 أنواع شائعة من Middleware جاهزة للاستخدام.
*   **توليد ذكي:** يمكنك توليد Middleware من مجرد وصف نصي بسيط.
*   **مصادر متعددة:** يدعم التوليد من JSON Schema أو قوالب مخصصة.
*   **واجهة تفاعلية:** واجهة سهلة الاستخدام ترشدك خطوة بخطوة.
*   **تحقق تلقائي:** يتم التحقق من صحة الكود المولد تلقائياً.
*   **توثيق شامل:** يتم توليد توثيق كامل مع كل middleware.
*   **قابل للتخصيص:** يمكنك بسهولة تعديل القوالب أو إنشاء قوالب جديدة.

---

## 3. ⚙️ التثبيت

المولد مدمج في نظام `php-magic-system` ولا يتطلب أي خطوات تثبيت إضافية. يمكنك البدء في استخدامه مباشرة من خلال `artisan`.

---

## 4. 🕹️ كيفية الاستخدام

### 4.1. الأمر الأساسي

الأمر الأساسي لتشغيل المولد هو:

```bash
php artisan generate:middleware
```

عند تشغيل هذا الأمر بدون أي خيارات، ستظهر لك قائمة تفاعلية ترشدك خلال عملية التوليد.

### 4.2. القائمة التفاعلية

القائمة التفاعلية هي أسهل طريقة لاستخدام المولد. ستطرح عليك الأسئلة التالية:

1.  **اختر طريقة التوليد:**
    *   من وصف نصي (Text Description)
    *   من JSON Schema
    *   من قالب (Template)

2.  **أدخل المعلومات المطلوبة:** بناءً على اختيارك، سيطلب منك إدخال الوصف، مسار الملف، أو اسم القالب.

3.  **اختر النوع (اختياري):** يمكنك تحديد نوع Middleware من قائمة الأنواع المدعومة.

4.  **أدخل الاسم (اختياري):** يمكنك تحديد اسم للكلاس، أو تركه فارغاً ليتم توليده تلقائياً.

5.  **معاينة وحفظ:** بعد التوليد، سيتم عرض النتائج ويمكنك تأكيد حفظ الملف.

### 4.3. خيارات سطر الأوامر

للمستخدمين المتقدمين، يمكن استخدام الخيارات التالية للتحكم الكامل من سطر الأوامر:

| الخيار | الوصف | مثال |
|---|---|---|
| `--name` | تحديد اسم Middleware. | `--name=ApiAuthMiddleware` |
| `--type` | تحديد نوع Middleware. | `--type=auth` |
| `--text` | التوليد من وصف نصي. | `--text="middleware for auth"` |
| `--json` | التوليد من ملف JSON. | `--json=path/to/schema.json` |
| `--template` | التوليد من قالب. | `--template=api-versioning` |
| `--save` | حفظ الملف المولد تلقائياً. | `--save` |
| `--validate` | التحقق من صحة الكود المولد. | `--validate` |
| `--list-types` | عرض قائمة بالأنواع المدعومة. | `--list-types` |

### 4.4. عرض الأنواع المدعومة

لمعرفة جميع أنواع Middleware التي يمكنك توليدها، استخدم الأمر:

```bash
php artisan generate:middleware --list-types
```

ستظهر لك قائمة بالأنواع العشرة المدعومة مع وصف لكل منها.

---

## 5. 📚 أمثلة عملية

### 5.1. مثال 1: توليد Authentication Middleware

**المتطلب:** إنشاء middleware للتحقق من وجود API Token في كل طلب.

**الحل:**

```bash
php artisan generate:middleware \
  --text="middleware للتحقق من المصادقة عبر API Token" \
  --type=auth \
  --name=ApiTokenAuthMiddleware \
  --save
```

**النتيجة:**
سيتم إنشاء ملف `app/Http/Middleware/ApiTokenAuthMiddleware.php` يحتوي على كود جاهز للاستخدام مع منطق التحقق من Token.

### 5.2. مثال 2: توليد Rate Limiting Middleware

**المتطلب:** إنشاء middleware لتحديد عدد الطلبات بـ 100 طلب في الدقيقة.

**الحل:**

1.  **توليد Middleware:**

    ```bash
    php artisan generate:middleware \
      --text="middleware لتحديد معدل الطلبات" \
      --type=rate_limit \
      --name=CustomRateLimiter \
      --save
    ```

2.  **تخصيص الكود:**
    افتح ملف `app/Http/Middleware/CustomRateLimiter.php` وقم بتعديل الخصائص:

    ```php
    protected int $maxAttempts = 100;
    protected int $decayMinutes = 1;
    ```

### 5.3. مثال 3: توليد من قالب مخصص

**المتطلب:** إنشاء middleware لإدارة إصدارات API.

**الحل:**

```bash
php artisan generate:middleware \
  --template=api-versioning \
  --name=ApiVersioningMiddleware \
  --save
```

**النتيجة:**
سيتم إنشاء ملف `app/Http/Middleware/ApiVersioningMiddleware.php` باستخدام القالب المخصص `api-versioning.php`.

---

## 6. 🔗 التسجيل في Kernel

بعد توليد أي middleware، **لا تنسى تسجيله** في `app/Http/Kernel.php` لتتمكن من استخدامه في الـ Routes.

```php
protected $middlewareAliases = [
    // ... existing middleware
    
    // Generated Middleware
    'api.token_auth' => \App\Http\Middleware\ApiTokenAuthMiddleware::class,
    'custom.rate_limiter' => \App\Http\Middleware\CustomRateLimiter::class,
    'api.versioning' => \App\Http\Middleware\ApiVersioningMiddleware::class,
];
```

---

## 7. 🚀 الاستخدام في Routes

بمجرد تسجيل الـ middleware، يمكنك استخدامه في ملفات الـ Routes (`routes/web.php` أو `routes/api.php`).

```php
// استخدام middleware واحد
Route::get('/profile', [ProfileController::class, 'show'])->middleware('api.token_auth');

// استخدام مجموعة middleware
Route::prefix('api/v1')->middleware([
    'api.token_auth',
    'custom.rate_limiter',
    'api.versioning',
])->group(function () {
    Route::get('/users', [UserController::class, 'index']);
});
```

---

## 8. 🎨 التخصيص المتقدم

### 8.1. تعديل القوالب

يمكنك تعديل القوالب الموجودة في `app/Templates/Middleware/` لتغيير الكود المولد لجميع أنواع Middleware.

### 8.2. إنشاء قوالب جديدة

1.  أنشئ ملف PHP جديد في `app/Templates/Middleware/`.
2.  اكتب كود القالب باستخدام المتغيرات المتاحة (مثل `{{name}}`, `{{namespace}}`).
3.  استخدم الأمر `--template` مع اسم القالب الجديد.

---

## 9. 🔍 استكشاف الأخطاء وإصلاحها

| المشكلة | الحل |
|---|---|
| **"Middleware not found"** | تأكد من تسجيل الـ middleware في `app/Http/Kernel.php`. |
| **"Class not found"** | تأكد من صحة الـ namespace واسم الكلاس. |
| **"Too many requests"** | اضبط إعدادات الـ Rate Limiting middleware. |
| **"Permission denied"** | تحقق من منطق الصلاحيات في الـ Permission middleware. |

---

## 10. 📞 الدعم

للمزيد من المعلومات أو المساعدة، يرجى مراجعة المستندات التالية:

*   [وثيقة التصميم (Design Document)](MIDDLEWARE_GENERATOR_DESIGN.md)
*   [أمثلة الاستخدام (Examples Guide)](MIDDLEWARE_GENERATOR_EXAMPLES.md)
*   [تقرير الاختبار (Test Report)](MIDDLEWARE_GENERATOR_TEST_REPORT.md)

---

**Generated by Manus AI**  
**SEMOP Team © 2025**
